### 一、vscode 下拉GitLab数据

 存储→储藏→拉取→弹出储藏（有冲突就解决） →暂存所有更改 →提交→提交已暂存的文件 → 同步

### 二、前端对接接口

1.在对应的文件夹api中新建对应的模块文件夹（ name），并创建.js（name.js）文件。

```js
import request from '@/utils/request'
export function getAll(data){  //设置接口函数名
    return request({
        url:'/oneStandardThreeFacts/siteInfo/v1/pc/list',  //将对应模块URL复制过来
        method:'get', //填写对应方式get，post，put，delete
       	data:'',  //对应系统模块接口的Request body
        params:data //对应系统模块接口的Parameters
    })
}
```

2.到模块组件中引入创建的接口函数

```js
import {getAll,deleteData} from '@/api/areainfo/policesstation.js'
```

3.到模块组件中找到对应操作的methods，比如

```js
getList() {
      // 查询接口，调用对应的接口函数
    //如果需要后端接收数据，则在方法函数中写入参数，并输出参看是否符合后台格式要求
      getAll(this.listQuery).then((res)=>{ //接口函数中对应的参数根据接口页面填写
        //console.log(res)  //控制台输出获得数据，并查看数据内容
        this.tableData=res.result.data
        this.total=res.result.total
      })
    },
```

### 三、GitLab上传代码

1、在本地文件夹 右键，点击Git Bash Here 进入git命令行模式.

2、在命令行输入 git clone url 克隆gitlab的项目在本地。url在gitlab上复制链接

3、进入新生成的目录，把要上传的文件复制进该目录下面

4、再一次“ Git Bash Here”  ，git命令窗口输入命令：git add . （命令的含义为把该文件夹下的所有文件均添加到		git上））

5、 git commit -m '注释' （提交代码到GitLab）

6、git pull （从远程仓库拉取代码  ），防止覆盖GitLab的数据

7、git push 提交到gitlab

### 四、学习github
1、git remote -v ======查看当前所有远程地址别名
2、git remote add 别名 远程地址 ==========起别名
3、git push 别名 分支 ========推送本地分支上的内容到远程仓库
4、git clone 远程地址======= 将远程仓库的内容克隆到本地
5、git pull 远程库地址别名 远程分支名 ========将远程仓库对于分支最新内容拉下来后与当前本地分支直接合并
